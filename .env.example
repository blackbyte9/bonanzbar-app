# Used by Prisma in local development. SQLite keeps setup zero-config.
DATABASE_URL="file:./prisma/dev.db"

# Production must set this to a cryptographically strong value.
AUTH_SECRET="replace-this-before-production"
